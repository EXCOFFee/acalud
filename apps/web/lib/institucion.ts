/** Valores reales de la tabla `levels` (backend). Usar siempre un Selector con estos valores
 *  en vez de texto libre: el backend valida por igualdad exacta contra `levels.name`. */
export const NIVELES_EDUCATIVOS = ['Inicial', 'Primaria', 'Secundaria'];
